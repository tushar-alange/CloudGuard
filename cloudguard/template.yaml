AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: CloudGuard - Serverless Security Compliance Automated Architecture

Parameters:
  AdminNotificationEmail:
    Type: String
    Description: Enter your personal email address to receive compliance alerts.

Resources:
  # 1. Decoupled Communication Topic
  CloudGuardSNSTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: cloudguard-security-alerts
      KmsMasterKeyId: alias/aws/sns # ✅ FIXES CKV_AWS_26: Enables encryption at rest using default AWS keys
      Subscription:
        - Endpoint: !Ref AdminNotificationEmail
          Protocol: email

  # 2. Serverless Computing Resource
  CloudGuardScannerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: app.lambda_handler
      Runtime: python3.14
      Timeout: 30
      ReservedConcurrentExecutions: 1 # ✅ FIXES CKV_AWS_115: Enforces execution limits to mitigate resource exhaustion
      Environment:
        Variables:
          SNS_TOPIC_ARN: !Ref CloudGuardSNSTopic
      # Give Lambda direct privileges to inspect IAM parameters
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - iam:ListUsers
                - iam:ListAccessKeys
                - iam:GetAccountSummary
              Resource: "*"
            - Effect: Allow
              Action:
                - sns:Publish
              Resource: !Ref CloudGuardSNSTopic
      # 3. Event-Driven Automated Cron Trigger
      Events:
        MidnightScheduledAudit:
          Type: Schedule
          Properties:
            Schedule: "cron(0 0 * * ? *)"
            Name: CloudGuardDailyCron
            Description: Automated trigger for the CloudGuard compliance engine
            Enabled: True
      
      # ✅ BYPASSES CKV_AWS_116, CKV_AWS_117, CKV_AWS_173: Documenting intentional architecture overrides
      Metadata:
        Checkov:
          Skip:
            - CKV_AWS_116: "Dead Letter Queue is not required for a stateless read-only automated cron script."
            - CKV_AWS_117: "VPC placement is unnecessary as the execution logic interacts solely with global AWS public endpoints."
            - CKV_AWS_173: "Environment variables only map basic resource ARNs, eliminating the requirement for custom KMS configurations."

Outputs:
  LambdaFunctionARN:
    Description: "CloudGuard Scanner Lambda Function ARN"
    Value: !GetAtt CloudGuardScannerFunction.Arn
  SNSTopicARN:
    Description: "CloudGuard SNS Topic ARN"
    Value: !Ref CloudGuardSNSTopic